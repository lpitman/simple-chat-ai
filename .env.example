# JWT Secret for signing and verifying tokens.
# Generate a strong, random string (e.g., using `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`)
JWT_SECRET=your_super_secret_jwt_key_here

# Initial Admin User Credentials (for your primary account)
INITIAL_USERNAME=admin
INITIAL_PASSWORD=adminpassword

# Optional: Guest User Credentials (uncomment and set if you want a guest account)
# GUEST_USERNAME=guest
# GUEST_PASSWORD=guestpassword
